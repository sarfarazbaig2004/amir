import 'package:flutter/material.dart';

class StatusLamp extends StatelessWidget {
  const StatusLamp({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}